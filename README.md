# 🛡️ Dotfiles Sentinel

Универсальный конфиг для Windows 11 (WezTerm + PowerShell 7 + Zed).

## 🚀 Быстрое развертывание (на новой системе)

1. **Установите Scoop:**
   `iwr -useb get.scoop.sh | iex`
2. **Установите Chezmoi:**
   `scoop install chezmoi`
3. **Инициализируйте конфиги:**
   `chezmoi init --apply https://github.com`

## 📦 Управление софтом

Все приложения разделены на два индекса в папке `~/.config/apps-index/`:
- **Scoop**: Основные инструменты разработчика (`scoop.json`).
- **Winget**: Системные тяжеловесы (`winget.json`).

### Бэкап софта:
Запустите скрипт синхронизации:
`& "$HOME\.config\apps-index\Sync-Apps.ps1"`

### Восстановление софта:
Скрипт запускается автоматически через `run_once`, но можно вызвать вручную:
`& "$HOME\.config\apps-index\Restore-Apps.ps1"`

## 🛠️ Полезные команды

- `ls` — расширенный листинг с иконками (eza).
- `lt` — дерево папок на 2 уровня.
- `glog` — красивое дерево Git.
- `lg` — интерактивный Lazygit.

### WezTerm Quake Mode
- **Win + ~** — Вызов терминала сверху.
- **Автозапуск**: Необходимо единажды запустить `wezterm_quake.exe` при первой установке, после чего он прописывается в системе самостоятельно.

## 📁 Структура .config
- `powershell/` — алиасы и автодополнение.
- `wezterm/` — конфигурация терминала.
- `zed/` — настройки редактора.
